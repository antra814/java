-an array is a data structure that can holds multiple values in a single variables.

syntax:-
     let fruits=["apple","banana","mango"];

-uses zero-based indexing.
-array has a dynamic size-can grow or shrink as needed.     